# ============================================================
# Deep Thinking Trading Bot — Open-Source Edition
# ============================================================
# NO OpenAI key needed — uses Gemini (free) + local embeddings
# ============================================================

# LangChain / LangGraph core
langchain
langgraph
langchain-community

# ── LLM Provider: Google Gemini (FREE tier available) ──
# Get API key at: https://aistudio.google.com/app/apikey
langchain-google-genai

# Pydantic + utilities
pydantic
rich
typing-extensions

# ── Market data tools ──────────────────────────────────────
yfinance          # Stock price data (supports .NS for NSE India)
finnhub-python    # Company news (free tier at finnhub.io)
stockstats        # Technical indicators (MACD, RSI, Bollinger Bands, SMA)
pandas
requests

# ── Web search ─────────────────────────────────────────────
tavily-python     # Live web search (free tier at tavily.com)

# ── Vector store for agent memory ──────────────────────────
chromadb

# ── Local embeddings (NO API KEY needed) ───────────────────
# Replaces OpenAI text-embedding-3-small
# Model (all-MiniLM-L6-v2, ~90MB) auto-downloads on first run
sentence-transformers

# ── Web scraping (dependency of some tools) ────────────────
beautifulsoup4

# ipython and pygraphviz are optional for visualization
ipython